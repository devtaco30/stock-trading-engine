spring:
  main:
    web-application-type: none
  # 정산=내구성 경계라 H2(인메모리, 재시작 시 소멸) 안 씀 — Postgres 필수(application-local.yml,
  # Jack이 직접 관리하는 gitignore 대상). datasource는 프로파일에서 채워야 하므로 여기 기본값 없음.
  jpa:
    hibernate:
      ddl-auto: update
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: settlement-worker
      auto-offset-reset: earliest
      enable-auto-commit: false
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "com.flab.stocktradingengine.kafka.event"
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      retries: 3
    listener:
      ack-mode: manual_immediate
